module Main

[<EntryPoint>]
let main argv =
    (*let device = Brahma.FSharp.ClDevice.GetFirstAppropriateDevice()
    let clContext = Brahma.FSharp.ClContext(device)
    let resize = GPUTools.resize clContext 64
    let myImage = BasicTools.load "/Users/svmena/Documents/GPUImageProcessing/tests/GPUImageProcessing.Tests/Images/input/4.jpg"
    let resizedMyImage = resize 100 100 GPUTools.Bilinear myImage*)

    0
